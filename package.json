{
  "name": "homelab",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "compile:tool": "./compile-tool.sh",
    "lint": "markdown-toc -i --bullets '-' --maxdepth 3 ./README.md && ts-import-fix -w && tsc --noEmit && biome check --write && adamhl8-knip",
    "prepare": "find .githooks -type f -exec ln -srf {} .git/hooks/ \\; || true"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "ts-explicit-errors": "^4.0.1"
  },
  "devDependencies": {
    "@adamhl8/configs": "^0.17.5",
    "@biomejs/biome": "^2.2.4",
    "@types/bun": "^1.2.23",
    "knip": "^5.64.1",
    "markdown-toc": "^1.2.0",
    "typescript": "^5.9.2"
  }
}
