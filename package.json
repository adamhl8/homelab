{
  "name": "homelab",
  "type": "module",
  "private": true,
  "module": "run.ts",
  "scripts": {
    "lint": "tsc --noEmit && biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@types/bun": "latest",
    "typescript": "^5.5.4"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
