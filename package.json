{
  "name": "homelab",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "compile:tool": "./compile-tool.sh",
    "lint": "tsc --noEmit && biome check --write",
    "prepare": "find .githooks -type f -exec ln -srf {} .git/hooks/ \\; || true"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "ts-explicit-errors": "^3.0.0",
    "untildify": "^5.0.0"
  },
  "devDependencies": {
    "@adamhl8/configs": "^0.10.1",
    "@biomejs/biome": "^2.1.4",
    "@types/bun": "^1.2.19",
    "typescript": "^5.9.2"
  }
}
