# For debian 13 "trixie"
# The `{{VAR}}` templates here are not a normal part of preseed. These are replaced by our serve.ts

d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i apt-setup/contrib boolean true
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/non-free boolean false
d-i hw-detect/load_firmware boolean true
d-i apt-setup/disable-cdrom-entries boolean true

### Clock
d-i clock-setup/utc boolean true
d-i time/zone string {{TIMEZONE}}
d-i clock-setup/ntp boolean true

### User
d-i passwd/root-login boolean false
d-i passwd/user-fullname string {{FULLNAME}}
d-i passwd/username string {{USERNAME}}
d-i passwd/user-password-crypted password {{PASSWORD_CRYPT}}

### Disk
# partman-auto/disk is not preseeded so we're forced to choose the correct disk
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic

# Do NOT auto-remove existing LVM/RAID. There is none (the NAS is ZFS), so this never fires -- but if d-i ever does find some, stopping beats destroying.
d-i partman-lvm/device_remove_lvm boolean false
d-i partman-md/device_remove_md boolean false

d-i partman/choose_partition select finish

### Packages
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string sudo curl ca-certificates gnupg
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

### Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default
d-i grub-installer/force-efi-extra-removable boolean false

d-i preseed/late_command string \
    in-target mkdir -p /home/{{USERNAME}}/.ssh; \
    in-target sh -c "echo '{{SSH_PUBKEY}}' > /home/{{USERNAME}}/.ssh/authorized_keys"; \
    in-target chown -R {{USERNAME}}:{{USERNAME}} /home/{{USERNAME}}/.ssh; \
    in-target chmod 700 /home/{{USERNAME}}/.ssh; \
    in-target chmod 600 /home/{{USERNAME}}/.ssh/authorized_keys

d-i finish-install/reboot_in_progress note
