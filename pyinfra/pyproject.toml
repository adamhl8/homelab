[project]
name = "homelab"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pyinfra>=3.9"]

[dependency-groups]
dev = ["ruff", "ty"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = ["I", "UP", "B"]
