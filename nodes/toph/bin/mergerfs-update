#!/bin/bash

curl -s https://api.github.com/repos/trapexit/mergerfs/releases/latest | grep -o -E "https://(.*)mergerfs(.*)ubuntu-focal_amd64.deb" | sed 1q | xargs curl -Lo ~/mergerfs.deb
sudo apt install ~/mergerfs.deb -y
rm ~/mergerfs.deb
mergerfs --version
