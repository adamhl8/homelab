{
	email adamhl@pm.me

	order authenticate before respond
	order authorize before reverse_proxy

	security {
		local identity store localdb {
			realm local
			path /etc/caddy/users.json
		}

		authentication portal authportal {
			enable identity store localdb
			cookie domain adamhl.dev
			cookie lifetime 172800
			crypto default token lifetime 86400

			ui {
				links {
					"Portal Settings" /settings icon "las la-cog"
				}
			}
		}

		authorization policy admin_policy {
			set auth url https://auth.adamhl.dev
			allow roles authp/admin
		}
	}

	admin :2019
}

*.adamhl.dev {
	tls {
		dns route53 {
			wait_for_propagation true
		}
	}

	@wildcard host *.adamhl.dev
	handle @wildcard {
		redir https://adamhl.dev
	}
}

auth.adamhl.dev {
	authenticate with authportal
}

joieparma.com {
	@resume path /resume
	handle @resume {
		root /srv/joieparma.com
		rewrite * /resume.pdf
		file_server
	}

	reverse_proxy joieparma-com.lan:8000
}

adamhl.dev {
	reverse_proxy adamhl-dev.lan:8000
}

watchtower.adamhl.dev {
	reverse_proxy adamhl-dev.lan:8001
}

battlegrind.adamhl.dev {
	reverse_proxy battlegrind.lan:8000
}

### Private Services ###

(private_service_base) {
	@authorized_ips remote_ip private_ranges 100.64.0.0/10
	handle @authorized_ips {
		{block}
	}

	authorize with admin_policy
	{block}
}

# Require auth unless the request is from LAN/Tailscale
(private_service) {
	import private_service_base {
		reverse_proxy {args[0]}
	}
}

# Services where the upstream forces HTTPS
# As of Caddy v2.11, proxying to an HTTPS upstream defaults the Host header to the upstream address (so Host agrees with the TLS SNI). But most apps want the original public Host, so we restore it with {host}.
# Without override -> Host: ucg-fiber.lan
# With override    -> Host: unifi.adamhl.dev (the original request Host)
(private_service_tls) {
	import private_service_base {
		reverse_proxy {args[0]} {
			header_up Host {host}
			transport http {
				{block}
			}
		}
	}
}

unifi.adamhl.dev {
	import private_service_tls https://ucg-fiber.lan {
		tls_server_name unifi.local
		tls_trust_pool file /var/lib/caddy/unifi-core.crt
	}
}

incus.adamhl.dev {
	import private_service_tls https://incus.lan:8000 {
		tls_server_name incus
		tls_trust_pool file /var/lib/caddy/incus-server.crt
		tls_client_auth /var/lib/caddy/incus-client.crt /var/lib/caddy/incus-client.key
	}
}

homepage.adamhl.dev {
	import private_service homepage.lan:8000
}

backrest.adamhl.dev {
	import private_service backrest.lan:8000
}

scrutiny.adamhl.dev {
	import private_service scrutiny.lan:8000
}

umami.adamhl.dev {
	@script path /imamu*
	handle @script {
		reverse_proxy umami.lan:8000
	}

	import private_service umami.lan:8000
}

actual-budget.adamhl.dev {
	import private_service actual-budget.lan:8000
}

filebrowser.adamhl.dev {
	import private_service filebrowser.lan:8000
}

papra.adamhl.dev {
	import private_service papra.lan:8000
}

immich.adamhl.dev {
	import private_service immich.lan:8000
}

jellyfin.adamhl.dev {
	import private_service jellyfin.lan:8000
}

qbittorrent.adamhl.dev {
	import private_service qbittorrent.lan:8000
}

radarr.adamhl.dev {
	import private_service radarr.lan:8000
}

sonarr.adamhl.dev {
	import private_service sonarr.lan:8000
}

prowlarr.adamhl.dev {
	import private_service prowlarr.lan:8000
}

seerr.adamhl.dev {
	import private_service seerr.lan:8000
}
